# Go build artifacts
*.test
*.out
/embedded/embedded.test

# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store

# Fetched native library artifacts — downloaded by `go run ./cmd/zeta-setup`
# or similar; not checked in (see README for distribution details).
/embedded/internal/lib/**/*.a
!/embedded/internal/lib/.keep
